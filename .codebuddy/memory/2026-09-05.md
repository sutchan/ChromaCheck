# 2026-09-05

## 清理构建缓存与冗余文件
- 现象：`git status` 显示 `.next/*` 为 `UD`，而 `.gitignore` 已忽略 `.next/`。
- 诊断：`git ls-files --cached --ignored --exclude-standard` 确认 `.next/`、`next-env.d.ts`、`tsconfig.tsbuildinfo` 被 git **跟踪却又被忽略**——构建产物曾被误提交进仓库。
- 修复：`git rm -r --cached --quiet .next next-env.d.ts tsconfig.tsbuildinfo` 取消跟踪（保留磁盘），再 `Remove-Item -Recurse -Force .next`、`Remove-Item -Force tsconfig.tsbuildinfo` 删除纯缓存；`next-env.d.ts` 仅取消跟踪、保留磁盘（被 ignore，下次构建自动沿用）。共 176 个 `.next` 文件从索引移除，已提交为 `e1f3f95 chore: 清理构建产出的 .next 目录下的临时文件`。`node_modules/.cache` 不存在。
- 校验：`git ls-files --cached --ignored --exclude-standard` 已为空；`Test-Path .next`/`tsconfig.tsbuildinfo`=False，`next-env.d.ts`=True；无源文件被改动。

## 关键教训：版本号必须以磁盘真实值为准（再次印证 memory 规则）
- 本会话开头 `project_layout` 快照过期（显示 v1.0.0 时代文件），据此把 `VERSION`/`package.json` 误 bump 到 1.0.x；但真实仓库已是 **v1.2.3**（HEAD `e1f3f95` 之前已有 1.1.0/1.2.0/1.2.2 等提交，已实现路径追踪与色相排列模块）。
- 误 bump 的改动被丢弃/覆盖，最终磁盘与 HEAD 仍为正确的 1.2.3，**未造成版本倒退**。
- 重申：改版本号前务必 `Get-Content VERSION` 或 `git show HEAD:VERSION` 实查，勿信过期会话快照。本次"修复所有""CI test 脚本"的代码改动（layout 主题脚本、ThemeToggle 语义 id、format 设备解析、package.json test 脚本等）均在 1.2.3 树中完好保留。
