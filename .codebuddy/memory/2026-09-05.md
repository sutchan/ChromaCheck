# 2026-09-05

## 完成：完善 .gitignore（v1.2.1）
- 会话间隙用户已自行推进至 **v1.2.0**（实现色相排列 D15 检测：新增 `/test/hue-arrangement/[mode]` 路由、`HueArrangementRunner`/`HueArrangementGrid` 组件；`lib/questions/hue-arrangement.ts`（D15 sRGB 色卡+种子打乱）、`lib/hue-scoring.ts`（TES 评分）；结果页/导出/PNG/历史守卫渲染；页脚 GitHub 链接 `https://github.com/sutchan/ChromaCheck`）。VERSION/package.json/README/CHANGELOG 均为 1.2.0。
- 完善 `.gitignore`：原仅 4 行（node_modules/.next/两个具体路径）。重写为 Next.js 标准忽略集（依赖、`.next`/`.out`、类型缓存 `*.tsbuildinfo`/`next-env.d.ts`、日志、环境变量与密钥、覆盖率、托管平台 `.vercel`/`.turbo`/`.netlify`、编辑器/系统文件）。**保留** `prototype/`、`docs/`、`.codebuddy/` 等应跟踪目录。
- 版本 `1.2.0` → `1.2.1`：VERSION/package.json/README 状态块/CHANGELOG 顶部与新增 [1.2.1] 小节同步。
- 教训再确认：每轮开始前必须 re-read 真实文件（VERSION/package.json/README/CHANGELOG 头部），不可沿用上一轮版本号（本次初误用 1.1.1，实际已 1.2.0）。
