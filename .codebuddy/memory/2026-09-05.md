# 2026-09-05

## 完成：v1.3.0 进阶联合判读（advanced 模式）实现
- 注意：会话间隙另一会话推进了 1.2.1–1.2.4（docs/ARCHITECTURE 治理），v1.3.0 从 1.2.4 bump。
- 新增：`lib/advanced-scoring.ts`（`computeAdvancedResult` 联合判读：三模块一致正常→置信度+4；ishihara 异常且进阶异常→置信度+8；ishihara 正常/样本不足但进阶异常→倾向疑似色弱 mild（conf 70/65）；分歧→注明复查。typeFromAdvanced：hue 优先（tritan→tritanomaly，否则按 TES 定 deutan 级），否则按 path targets protan/deutan 失败数）；`components/test/IshiharaFlow.tsx`（从 TestRunner 抽取的可复用答题流程，props: questions/initial/onProgress/onDone，持久化由外层 onProgress 回调决定）；`components/test/AdvancedRunner.tsx`（三阶段 chip 指示：phase0 IshiharaFlow、phase1 循环 PathTrackingCanvas、phase2 HueArrangementGrid，最后 scoreIshihara+computeAdvancedResult→saveResult→/result/[id]）；`app/test/advanced/page.tsx`（无 [mode] 段）。
- 重构：TestRunner 变薄（~75 行，onProgress 持久化 + key 重挂 restart），标准模式对外行为/持久化语义不变；TestMode 扩展 'advanced'（types/scoring MODE_TEXT/storage TestProgress.mode→TestMode）；三个 [mode] 页守卫保持拒绝 advanced（独立路由）。
- 接入：test/page.tsx 进阶卡启用（/test/advanced，约 15 分钟）；history 标签「进阶联合」（三字段同存时）；结果页/导出无需改动（三字段守卫渲染自动覆盖 advanced）。
- 设计决策：advanced 不做跨阶段进度持久化（仅 startedAt），中途刷新需重做当前模块之后流程。
- 版本 1.2.4→**1.3.0**：VERSION/package.json/CHANGELOG（1.3.0 节）/README（状态行+功能清单+结构树）/Footer v1.3.0；SPEC（§0/§1.2/§5.5/§9）/DATA-SPEC（testMode×3）/PRD/PRIVACY/API/ARCHITECTURE/ROADMAP 声明同步「advanced 已于 v1.3 实现，推迟仅剩匿名分析」。
- 验证：npm test（tsc --noEmit）通过、read_lints 0 错误；**next build 本轮被环境跳过未跑**，提交前需补跑（先设 CODEBUDDY_SAFE_DELETE_ENABLED='0'）。
- 项目剩余规划：仅匿名分析 analyticsEnabled（与本地优先隐私口径冲突，默认不做）。

## 完成：v1.4.0 趣味性体验包（FUN 模式）
- 趣味包代码由中间会话提交为 `9432732 feat: add fun mode experience and related features`（源文件：lib/fun.ts、lib/sharecard.ts、components/test/FunBits.tsx、components/result/EyesSwitcher.tsx、components/result/DimScenes.tsx，及 IshiharaFlow/ThemeProvider/ThemeToggle/ReportActions/result 页/TestRunner/AdvancedRunner 改动）。
- 本会话仅做版本落地：1.3.0→**1.4.0**（VERSION/package.json/Footer v1.4.0/storage & types 头注释）；README 状态行+功能清单+结构树（fun.ts/sharecard.ts/FunBits/EyesSwitcher/DimScenes）；CHANGELOG 新增 [1.4.0] 节；SPEC §3.5、ROADMAP 趣味性体验包标注 v1.4 已实现。提交 `57f7a5c`。
- **7 功能**：进度点阵(ProgressDots,三态不泄露对错,每8题脉冲+「已完成 N/24 版」)、章末科普过渡(ChapterCard,3章,回车/按钮继续)、中性提交反馈(✓ 已记录 420ms 恒开)、色觉人格分享卡(sharecard.ts Canvas PNG,六轴雷达=三轴维度+转换/分类错误率+消失错误率+综合)、换一双眼睛(EyesSwitcher,第4版答案29 五视角复用 IshiharaPlate cvd)、三轴互动科普(DimScenes,aria-expanded)、旅人隐喻称号(TITLES)。**总开关** AppSettings.funMode(默认开,ThemeToggle 处"趣味开/关")。
- **红线已遵循**：答题不显对错(仅 done/cur/pending 三态)、无积分排行、无障碍零豁免(aria/键盘可达)、称号去污名化仅本机。
- 验证：npm test(tsc)通过、read_lints 0 错误；**next build 再次被环境跳过未跑**（PowerShell 设 CODEBUDDY_SAFE_DELETE_ENABLED='0' 后仍跳过，tsc+lint 为实际闸门）。
- 项目剩余规划：仅匿名分析 analyticsEnabled（与本地优先隐私口径冲突，默认不做）；ROADMAP 仍有 P1（38 板图谱、分享卡片社交优化、屏幕校准指引）、P2（英文版、PWA）。

## 检查示例文字 + 重新执行 ARCHITECTURE 规划残留清理（v1.2.4）
- 用户任务：检查所有文档与代码是否还有"示例文字"（占位/模板文字）。
- **示例文字检查结论：无**。全仓（docs/*.md、README、app/components/lib）无 lorem ipsum、无"示例文字"占位、无模板占位词（your-/ACME/公司名称等）。命中项均为正常内容：API/测试/部署文档的"请求/响应示例"、功能描述"占位图/占位状态"、代码 `placeholder="输入数字"`（真实输入框提示）、`result?id=xxx`（URL 参数示例）、history 空态文案"结果会显示在这里"。
- **重要发现（上轮编辑会话压缩后未持久化）**：上轮"继续清理 ARCHITECTURE 规划残留（bump 1.2.4）"整体丢失——磁盘回退到 1.2.3，CHANGELOG 仅到 [1.2.3]，ARCHITECTURE 仍含 shadcn/ui、Recharts、@dnd-kit、TestContext、jsPDF/html2canvas 等虚构内容。本轮重新执行清理，并 re-read 验证真正持久化（VERSION=1.2.4、CHANGELOG 有 [1.2.4] 小节、§2.1/§2.3/§2.5 已修正）。
- ARCHITECTURE 清理：§2.1 删 shadcn/ui/Recharts/@dnd-kit、加"内联 SVG"行；§2.3 检测状态改组件本地 state、删 TestContext 数据结构块；§2.5 去 jsPDF/html2canvas/@dnd-kit 动态导入。
- 版本 `1.2.3` → `1.2.4`：VERSION/package.json/README/CHANGELOG 同步。
- 教训固化：**编辑后必须 re-read 验证持久化**；会话被压缩/恢复后，未真正落盘的改动会静默丢失（工具报 succeeded ≠ 已落盘）。另：daily 本文件在压缩中曾被清空，已重建。
- 遗留 cosmetic：删除 TestContext 块后 §2.3 末尾留有 3 个连续空行（Markdown 渲染等价为单一分隔，不影响正确性，未强行清理因反引号干扰精确替换）。

## 完成：v1.5.0 石原氏 38 板完整图谱
- 任务来源：用户从 ROADMAP P1 选定「38 板完整石原氏图谱」（其余 P1：分享卡片社交优化、屏幕校准指引；P2：英文版/PWA 仍规划中）。
- 实现：`lib/questions.ts` 原有 24 题后新增 plate 25–38 共 14 道（转换×3、消失×4、隐藏×2、分类×3、演示×1、常规×1），六类题型全覆盖；快速版维持 10 题核心集（quick 不变），标准版与进阶版检测量 24→38。
- 图版视觉由 `seed={q.plate}` 驱动（IshiharaFlow 第148行），新增板号自动生成独立点阵，无需改渲染逻辑；`figureText` 隐藏题返回 ''（正常视觉空白），其余画 answer，数据模型原生支持扩容。
- 展示文案 24→38 同步：app/test/page.tsx（标准/进阶 meta 与 desc）、app/page.tsx（首页 FEATURES）、app/layout.tsx（description×2）、lib/advanced-scoring.ts、components/test/AdvancedRunner.tsx（PHASES）；文档：docs/SPEC.md（§检测模式、CC.questions）、docs/PRD.md（题量分布）、docs/ROADMAP.md（功能范围 + 38 板标注 v1.5 已实现）、README.md（版本号+功能特性）。
- 版本 1.4.0→**1.5.0**：VERSION/package.json/CHANGELOG([1.5.0] 节)/README 状态行/改动文件头注释；doc 头版本注释保持各自文档版未刷写（跨项目规则）。
- 验证：tsc --noEmit 通过（exit 0）；node 脚本核对 QUESTIONS=38、plate 1–38 连续、quick=10。未跑 next build（环境 shim 坑，tsc+lint 为实际闸门）。
- 决策：prototype/assets/js/data.js 的 CC.questions 维持 24 题演示数据与 demoResult/demoAnswers 不变（原型为解耦演示，改之会破坏其 demo 逻辑），未同步 38 板。

## 完成：v1.5.1 接入 Google Analytics 4（衡量 ID G-0F9QWS1PDX）
- 用户要求接入 GA（此前「匿名分析与本地优先隐私口径冲突、默认不做」的结论被显式覆盖）。
- 新增 `lib/analytics.ts`：`GA_MEASUREMENT_ID`（默认 G-0F9QWS1PDX，`NEXT_PUBLIC_GA_ID` 可覆盖；`NODE_ENV !== 'production'` 时为空→不加载）、`pageview(path)`、`trackEvent(name, params)`；脚本未就绪时静默跳过（首次 PV 由 gtag('config') 自动上报，跳转补报）。
- 新增客户端组件 `components/analytics/GoogleAnalytics.tsx`：`next/script` strategy=afterInteractive 注入 gtag/js + 内联初始化（`anonymize_ip: true`），`usePathname` 变化补报 page_view；挂载于 `app/layout.tsx` body 首位。
- 文档同步（原「无任何数据上报 / 不嵌入追踪脚本」口径已过期）：`app/privacy/page.tsx` 新增第 6 节并更正 §2 旧表述、更新日期 2026-09-05；`docs/PRIVACY.md` §2.2 重写为 GA4 采集范围与不上报清单；`docs/SPEC.md`（隐私段 + 推迟项：GA4 已实现、`analyticsEnabled` 仍推迟）；`docs/ARCHITECTURE.md`（§5 上报隐私要求加 v1.5.1 实际说明、§9.1 监控加 GA4）；`docs/DEPLOYMENT.md` 两处环境变量表加 `NEXT_PUBLIC_GA_ID`；`docs/ROADMAP.md` 数据反馈行标注；README 版本/隐私段/结构树。
- 版本 1.5.0→**1.5.1**（VERSION/package.json/CHANGELOG [1.5.1] 节/改动文件头注释 layout.tsx、privacy/page.tsx，新文件标 v1.5.1）。
- 验证：tsc --noEmit 通过；read_lints 无新增错误（仅既有 no-page-custom-font warning）；`next build` 成功（先设 CODEBUDDY_SAFE_DELETE_ENABLED='0'，17 页生成）。
- 未做：设置项内的「允许统计」UI 开关（用户未要求；停用方式为用户侧脚本拦截或 `NEXT_PUBLIC_GA_ID` 置空）。
