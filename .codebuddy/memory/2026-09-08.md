# 2026-09-08

## 原型对齐应用算法 + 题库同步（prototype/）
- 背景：09-06 审查发现原型判读算法与应用已分叉（原型 24 题绝对阈值、色相恒 deutan、路径容差 0.055 命中率）。
- 本次把 `prototype/` 对齐到应用当前实现，使原型判读行为正确：
  - `data.js` 题库 24→**38 题**（逐字对齐 `lib/questions.ts` v1.7.4，含 plate 25–38）；`demoResult` 明细改 `25/38`、误差模式 deutan:10/protan:5/mixed:1；`demoAnswers` 扩至 38 条。
  - `scoring.js` 总体结论改**错误率阈值**（wrongRate>=0.6 色盲 / >=0.3 色弱），对齐 `lib/scoring.ts`；`demoWrong` 兜底层用 wrongRate<0.3。
  - `scoring.js` `scoreHue` 偏差方向加**红绿/蓝黄轴启发式**（min(相邻)<=5 → 蓝黄），不再恒 deutan，对齐 `lib/hue-scoring.ts`；注意原型 `seqOf` 已含两端 0/14，故 scoreHue 直接以入参为 seq（不重包）。
  - `scoring.js` `scorePath` 改 **IoU + 重采样 80 点 + TOL 0.07**，对齐 `lib/path-scoring.ts`（`PASS=70` 经 `CC.rules.pathOverlap.pass`）。
- 版本（原型自洽 bump，不碰应用 VERSION/package.json）：`data.js` v0.1.3→v0.1.4、`scoring.js` v0.1.0→v0.1.1、`prototype.html` v0.1.1→v0.1.2（含定位声明改为"已对齐 app/lib"）、`app.js` 动态 title 同步 v0.1.2。
- 验证：Node 无浏览器跑 data.js+scoring.js —— 题库 38/quick 10；demo 判读 suspected_deficiency/deuteranomaly/mild/85/25-38；全对 normal/100；全错 suspected_blindness/deuteranopia/severe；色相正序 TES0 normal/none、逆序 TES13 normal/none；路径重合 100、空 0。校验脚本 `_verify.mjs` 已删。
- 未提交（按规则不主动 commit）。原型仍为视觉/交互验证稿，但判读已与应用一致。
