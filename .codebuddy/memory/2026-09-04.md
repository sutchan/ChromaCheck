# 2026-09-04

## ChromaCheck 高保真原型搭建（v0.1.0 起点）
- 在 `prototype/` 下从零搭建纯静态原型集（无构建依赖），三页互相链接：
  - `prototype.html`：高保真可交互产品原型（设备画框切换 + 色觉模拟 + 真实题库/判读数据）
  - `wireframes.html`：组件库规范（基础/复合/业务组件 + 使用规则 + 无障碍基线）
  - `design-system.html`：设计系统（色彩/字体/间距/圆角/阴影令牌 + 色觉模拟基线）
- 样式分层：`tokens.css`（设计令牌）/ `ui.css`（基础组件）/ `screens.css` / `screens-test.css`（业务屏）/ `docs.css`（规范页外壳）
- 逻辑分层：`data.js`（真实题库）/ `scoring.js` / `ishihara.js`（`CC.renderPlate`/`renderPathField`/`simulate`）/ `charts.js` / `screens-*.js`（屏幕渲染）/ `wireframes.js`（组件目录渲染+轻量 toast/modal）/ `design-system.js`（自包含色觉模拟矩阵）
- 组件目录数据：`wireframes-catalog-base.js` / `-comp.js` / `-biz.js` 各自 push 到 `window.CC_CATALOG`
- `prototype.html` 外壳已加入跳转到 wireframes/design-system 的入口
- 注：ChromaCheck 仓库当前无 `package.json` / `VERSION` 单一版本源，原型文件头统一 v0.1.0
- README 已补充「高保真原型」章节（入口 + 目录结构）

## ChromaCheck 文档审查（发现的问题/缺陷/不足）
仓库现状：仅 `docs/`(9份.md) + `prototype/`(纯静态原型) + README，**无 package.json / app/ / components/ / lib/ / types/ / LICENSE**。
- 【严重】全部文档（README/PRD/ARCH/API/DATA-SPEC/DEPLOY/Test/Contrib/Privacy/Roadmap）描述的是尚未实现的 Next.js 应用，未声明「实现状态」，README 第98-175行项目结构树完全虚构；按 README 跑 `npm install/dev` 必失败。
- 【严重】README 第266行 `[MIT](LICENSE)` 链接目标 LICENSE 文件缺失。
- 【重要】数据结构前后矛盾：PRD§6 vs DATA-SPEC§2/4/5/6（IshiharaQuestion 的 type 枚举/timeLimit↔suggestedTime/tritanopiaAnswer/inQuickSet；TestResult 的 testType↔testMode/correctCount 位置/assessment↔ishiharaAssessment+overallAssessment；LocalStorageData 的 theme(system)/analyticsEnabled/confidence；AnswerRecord 缺正确答案/是否正确）。
- 【重要】判读算法矛盾：ARCHITECTURE§3.2 vs DATA-SPEC§7（演示题扣分 -20↔-25；设计题 -20 仅ARCH有；答题数<50% -20 仅DATA-SPEC有；程度判定逻辑两套不互补）。
- 【重要】API§3.2 AnalyticsEvent 比 ARCHITECTURE§5.2 多 `eventId`，且 event 枚举多 `learn_view`。
- 【重要】范围冲突：PRD 把路径追踪(F4)/色相排列(F5)列为核心，ROADMAP§2.2 把它们排除在 v1.0 外。
- 【重要】DATA-SPEC§4/5 引用 `ColorDeficiencyType/OverallResult/SeverityLevel` 未定义。
- 【一般】DEPLOYMENT§2.2 步骤1 `git init` 模板残留；TESTING§4.3 文案「开始免费检测」与 PRD「开始检测」不符；PRD 首屏<2s 但 Test/Deploy LCP<2.5s；PRD「1000并发」与纯前端架构矛盾；Test§3.1 提 DimensionBar 组件未列于组件清单；文档统一 v1.0 但原型头 v0.1.0，且无 VERSION/CHANGELOG 单一版本源；CI 工作流 .github/workflows/ci.yml 被引但不存在；各文档负责人为 `—`。

## 创建项目规范总纲 docs/SPEC.md v0.1.0
- 新建 `docs/SPEC.md` 为**权威总纲**（单一事实来源），整合 9 份分册 + 原型真实事实，并解决前次审查发现的矛盾。
- 关键决议（SPEC 为最终权威）：① 实现状态：文档+静态原型阶段，Next.js 应用未实现；② 数据结构以原型真实字段为准，统一枚举（ColorDeficiencyType/OverallResult/SeverityLevel/TestMode）；③ 判读算法以 `prototype/assets/js/scoring.js` 真值为准（演示题扣分取 -25/DATA-SPEC）；④ 功能范围按 ROADMAP：v1.0 排除路径追踪(F4)/色相排列(F5)，且补入隐私政策页；⑤ API 事件以 API§3.2 为准（含 eventId/learn_view）；⑥ 设计令牌以 `prototype/assets/css/tokens.css` 为单一来源，品牌色棱镜靛 #4b3df0。
- 原型真实契约已固化：CC.questions(24题,6 type)/CC.rules/CC.scoreIshihara/scoreHue/scorePath/CC.renderPlate/renderPathField/simulate(ishihara.js Viénot-Brettel 近似)。
- README 已加「实现状态」声明 + SPEC 入口链接，与 SPEC §0 对齐。
- 下一步建议：据 SPEC 修订 9 份分册以消除字段/算法/范围冲突；确立版本单一来源（package.json/VERSION）；补 LICENSE、.github/workflows/ci.yml。

## 据 SPEC 修订 9 分册 + 补缺失文件（已完成）
- 9 份分册顶部已加「规范遵循 SPEC + 实现状态（文档+静态原型阶段，Next.js 未实现）」块。
- PRD：F4/F5 标注 v1.1+ 实现（对齐 SPEC §1.2）；§4.1「1000 并发」→纯前端静态托管可水平扩展；§6.1/§6.2/§6.3 字段对齐 SPEC（plate/answer/protan/deutan/quick、testMode/ishihara、theme 三值/analyticsEnabled/confidence）。
- ARCHITECTURE：§3.1 `q.quickSet`→`q.quick`；§3.2 判读算法对齐 SPEC §6（演示题 -25、移除设计题 -20、统一程度/置信度四因子）；§5.2 AnalyticsEvent 对齐 API（补 eventId、枚举用 page_view/cta_click/answer_submit/test_complete/share/download/test_error/learn_view）。
- DATA-SPEC：§2.1 IshiharaQuestion 字段对齐原型真实结构（plate/answer/protan/deutan/quick，移除 plateNumber/imageUrl/correctAnswer/inQuickSet/suggestedTime/tritanopiaAnswer，蓝色弱用 answerTritan?）；§3 AnswerRecord 补 correct/expectedAnswer。原型 data.js 实证字段：id/plate/type/answer/protan/deutan/difficulty/quick。
- DEPLOYMENT：§2.2 修正 `git init` 误用（已用 Git，禁止 git init 覆盖）。
- TESTING：§4.3 文案「开始免费检测」→「开始检测」对齐 PRD CTA。
- CONTRIBUTING：顶部署「开发环境 npm 流程仅适用于实现阶段」注。
- 补文件：根 `LICENSE`（MIT，Copyright 2026 ChromaCheck Contributors，对齐 README 链接）；`.github/workflows/ci.yml`（lint+tsc+test+build+playwright，注明应用骨架未建暂不生效）。
- 仍待后续：确立版本单一来源（package.json/VERSION）；各分册负责人补实；文案/指标（首屏<2s vs LCP<2.5s）可进一步统一。

## ChromaCheck 设计系统/组件库/交互标准补全（原型集四页建成）
- 新增 `prototype/assets/js/icons.js`：`CC.icon(name)` 内联 24×24 线性 SVG 图标库（4 组 31 个：导航/操作/检测/反馈），`currentColor` 继承、色觉可辨；`CC.ICON_GROUPS` 供设计系统展示。
- `design-system.html` 补两节：#sec-icons（图标库，JS 渲染分组画廊）+ #sec-motion（动效：缓动/时长令牌 + 5 类动效卡片 + 可交互 demo：进入/进度/骨架/脉冲），`design-system.js` 同步扩展渲染与 demo 接线。
- 新增 `prototype/interaction.html` + `assets/js/interaction.js`：**交互标准**页，四类规范均可交互演示——
  - 模式：导航/路由标签、数字键盘输入、单选芯片、骨架加载占位
  - 反馈：Toast(成功/信息/警告/错误)、进度+完成、即时确认
  - 错误：表单内联错误、阻断式错误模态(Esc关闭/重试)、网络错误状态卡
  - 空状态：无历史、首次引导、筛选无结果、离线
- 三页导航（`prototype.html` 外壳 / `wireframes.html` / `design-system.html`）均加入「交互标准」入口；四页互相连通。
- `docs.css` 追加：图标库、代码块、动效 demo、交互组件（tabs/keypad/chip/toast/modal/field/state-card）全套样式。
- README「高保真原型」表与目录结构已补 interaction.html 行；设计系统说明补「图标/动效」。
- 校验：`icons.js`/`design-system.js`/`interaction.js` 经 lint 0 错误。原型仍无 package.json/VERSION 单一版本源，文件头维持 v0.1.0。
